   



新版BLE蓝牙主从机 首次使用需要将两个模块同时上电，待主从机模块灯同时快闪时，断开从机电源，主机不动，再将从机重新上电，等几秒两模块会自动连接，如果有数据传输，主从机灯会同时同频闪烁，没有数据传输，主从机灯会常亮，以后不再需要同时上电，两模块也可以连接。（操作完成后如果不同时上电不能连接可重复以上操作或按手册中下载资料中的虚拟串口助手，使用虚拟串口助手连接成功后再重复以上操作即可）
